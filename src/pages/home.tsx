import { FC } from "react";
import {
	HeroContainer,
	SectionContainer,
	FeatureContainer,
} from "../containers";

export const Home: FC = () => {
	return (
		<>
			<HeroContainer />
			<SectionContainer />
			<FeatureContainer />
		</>
	);
};
